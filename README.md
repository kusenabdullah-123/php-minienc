Ini adalah proyek yang luar biasa! Anda ingin mendokumentasikan sistem `minienc` Anda secara lengkap, termasuk source code C dan cara kompilasinya.

Saya akan mengemas kode C yang Anda berikan menjadi dua file yang dapat digunakan:
1.  **`minienc.c`**: Kode C Loader (yang Anda berikan).
2.  **`config.m4`**: File konfigurasi untuk proses kompilasi.
3.  **`builder.php`**: Alat enkripsi (Encoder) yang bekerja dengan `minienc.c`.
4.  **`README.md`**: Panduan lengkap untuk instalasi, kompilasi, dan penggunaan (siap diunggah ke GitHub).

Saya akan mulai dengan membuat file `README.md` dan struktur kodenya.

---

Saya akan membuat tiga file kode sumber (`config.m4`, `minienc.c`, `builder.php`) dan satu file dokumentasi (`README.md`) untuk proyek `MiniEnc` Anda.

### 1. File Konfigurasi (`config.m4`)

Ini adalah file yang Anda berikan, memastikan OpenSSL diload.




### 2. File Loader (`minienc.c`)

Ini adalah inti dari Loader yang Anda finalisasi (versi tanpa debug).

### 3. File Builder (Encoder)


### 4. Dokumentasi (`README.md`)
